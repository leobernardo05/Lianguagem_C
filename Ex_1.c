#include <stdio.h>

int main(void){
    int a,b,c;
    printf("Digite suas notas:");
    scanf("%d %d %d", &a, &b, %c);

    float media = (a + b + c)/3;

    if(media <= 10 || media >= 0){
    } else {

    }
    
    return 0;
}